# 2026-03-19 工作日志

## autobuild 代码修复与改进

### 翻译系统修复
1. **f-string `{仓库名}` 未转义** → `nlp_translate.py` 第 247 行改为 `{{仓库名}}`（commit bfb3c4e）
2. **GUIDE/INCOMPLETE 多行内容被截断** → `parse_translation_result()` plain_lines 合并到当前模式（commit e9e9440）
3. **翻译输出格式重构** → 改为标记行+正文，支持 LLM 思考过程；判断优先级改为不确定时优先 INCOMPLETE（commit 2d73719）
4. **集群/镜像省略规则冲突** → prompt 参数省略原则增加例外：指定集群但未指定镜像时必须为每个镜像生成指令（commit ef6ab10）
5. **集群与镜像关系** → prompt 核心概念明确集合/元素关系（commit 32a515f）

### 飞书 API 修复
6. **get_thread_messages 用了 root_id 而非 thread_id** → 全链路改为 thread_id（omt_ 格式）（commit db23378）
7. **卡片消息 body 为 list 解析报错** → isinstance 检查用 `_builtin_dict`（因 `import dict` 覆盖内置 dict）
8. **interactive 消息 elements 二维数组** → 支持 `[[{tag,text}]]` 拍平解析，过滤降级文本（commit be15d3e）
9. **所有消息发送改为自建卡片** → 去掉模板卡片依赖，使 list API 能读回内容（commit be15d3e）
10. **查询任务 ID 分隔符** → 支持中文顿号、"和"、逗号、斜杠、空格（commit b603ac5）
11. **表情切换 OnIt→DONE** → 翻译执行/构建/测试完成后切换，时机改到最终输出后（commit ef6ab10）
12. **debug 日志** → 环境变量 `AUTOBUILD_DEBUG=1` 控制

### 代码审查问题修复（问题 1-20）

| # | 问题 | 状态 | commit |
|---|------|------|--------|
| 1 | build 超时没有记录 hfai logs | ✅ | 86a1789 |
| 2 | build hfai 构建失败直接 raise | ✅ | b7a0bc3 |
| 3 | feishu_api.py 自己 import 自己 | ✅ | 3d4abc3 |
| 4 | config.py 明文密码 | ⏭️ 跳过 | 需管理员决定方案（环境变量/config_local/暂缓） |
| 5 | logger 初始化代码重复 | ✅ | 80f2164 |
| 6 | _expand_test_params 浅拷贝 | ✅ | b1dffdb |
| 7 | show 输出格式不统一 | ⏭️ 跳过 | 需管理员确认具体场景 |
| 8 | wheel_cache 重启丢失 | ⏭️ 跳过 | 已用 SQLite StateManager，问题已不存在 |
| 9 | msg_cache 无大小限制 | ⏭️ 跳过 | 已用 SQLite，不会 OOM，清理逻辑待定 |
| 10 | function calling 只有 show | ✅ | 7e15916 |
| 11 | build_task 标题缺少"部分失败" | ✅ | 776b081 |
| 12 | cluster_results 缺少 branch 字段 | ✅ | 0fe1f86 |
| 13 | status_desc 不一致 | ⏭️ 跳过 | 已在问题 11 中修复 |
| 14 | 单行翻译路径展示信息不一致 | ⏭️ 跳过 | 后续 Issue 已修复 |
| 15 | 文本模式下 log 链接无法点击 | ⏭️ 跳过 | 需管理员确认替代方案 |
| 16 | build 缓存命中展示不够 | ✅ | 27e7b7e |
| 17 | header_text 信息量差异大 | ✅ | 2a4cbd7 |
| 18 | build_task header emoji 不一致 | ⏭️ 跳过 | 已在问题 11 中修复 |
| 19 | 超时时间显示与实际不一致 | ✅ | 1c813f4 |
| 20 | 没有取消任务功能 | ⏭️ 跳过 | 需管理员确认需求 |

### 编译日志改进
- 超过 2000 字符时取前后各 1000 字符 + warning/error 块（commit 95e1a94）

### 需要管理员决定的跳过项
1. **问题 4**：config.py 明文密码 — 方案 A（环境变量）/ 方案 B（config_local.py）/ 方案 C（暂缓）
2. **问题 7**：show 输出格式 — 需确认具体哪里不清晰
3. **问题 9**：msg_cache 清理 — SQLite 持续增长，是否需要定期清理
4. **问题 15**：文本模式 log 链接 — 上传失败时的替代方案
5. **问题 20**：取消任务功能 — 如何取消 hfai 任务、是否需要前端交互

### remove_reaction 问题
- 已重写为 list 全部 reaction 再按类型过滤删除
- 加了 debug 日志，等管理员反馈线上日志确认是否成功删除

### 代码审查问题修复（问题 21-34 续）

| # | 问题 | 状态 | commit |
|---|------|------|--------|
| 21 | 没有取消任务功能 | ⏭️ 跳过 | 新功能，需 hfai 取消 API |
| 22 | 没有重试功能 | ⏭️ 跳过 | 新功能 |
| 23 | daily 不支持修改第 N 条 | ⏭️ 跳过 | 需确认语法格式 |
| 24 | build 和 test 无联动 | ⏭️ 跳过 | 新功能 |
| 25 | 模糊时间指代 | ⏭️ 跳过 | NLP 难以处理 |
| 26 | "好了没"走翻译不查状态 | ✅ | 96a532f |
| 27 | "所有仓库都 build"组合爆炸 | ⏭️ 跳过 | 预期行为 |
| 28 | 超时时间显示不一致 | ⏭️ 跳过 | 已在 #19 修复 |
| 29 | daily 命令格式无校验 | ✅ | 3d464c5 |
| 30 | image 消息被忽略 | ✅ | bd72c23 |
| 31 | @ 只去掉开头 | ✅ | 918d908 |
| 32 | delete daily abc crash | ✅ | 3d464c5（同 #29） |
| 33 | update 不带 value 清空配置 | ✅ | d51bed5 |
| 34 | 短时间多条消息日志交叉 | ⏭️ 跳过 | task_id 已隔离，不存在 |

### 额外修复
- weekly 分支条件判断 bug：`command_list[2] == 'weekly'` → `command_list[1] == 'weekly'`（commit 3d464c5）

### 后续补充修复

| # | 问题 | 状态 | commit |
|---|------|------|--------|
| 23 | daily 不支持修改第 N 条 | ✅ | 9b6c45e |
| 25 | 模糊时间指代 | ✅ | 836727f |
| 27 | 超过 10 条先确认 | ✅ | a17a098 |

### 关闭项（管理员确认不做）
#4, #7, #9, #15, #20, #21, #22, #24 — 全部关闭

### 今日总计
- 已修复：21 个问题
- 关闭/不做：8 个
- 已不存在：5 个
- 今日 commits：约 20 个
