# 2026-03-20 日记

## autobuild 代码审查与修复

管理员让我对 autobuild 仓库（yukuai26/autobuild）进行全面代码审查和修复。

### 今天完成的修复（按提交顺序）：

1. **show 工具截断限制** 3000 → 50000 字符
2. **禁用 LLM 自动分析** — build 失败和 query 不再喂 LLM，直接返回原始日志
3. **query/get_context 截断** 也调至 50000
4. **nccl 构建产物** — cache 存储和读取使用完整产物行，log 记录所有构建产物
5. **query 工具** — 增加 task_id 格式校验和明确错误提示
6. **wheel_cache 去掉 build_log** — 直接读 log 文件，减少 SQLite 膨胀
7. **parse_update all_ 前缀** — 用 _parse_override_key 正式解析替代硬编码
8. **clusters 限制为单集群** — 不支持逗号分隔，改为必填参数
9. **NLP prompt 重写** — clusters/image 展开规则清晰分条
10. **build_force 指令** — 跳过 cache 强制构建，预处理方式避免 export 冲突，后改为独立指令关键字
11. **缓存命中显示** — 统一"点击查看 log"文案，修复超链接模式下括号匹配不上的 bug
12. **缓存命中 log 回退** — 当前 task_id log 为空时读原始任务 log
13. **stdout 日志截断** 500/200 → 2000
14. **_validate_response 副作用 bug** — parse_command 加 dry_run 参数，校验时不执行修改
15. **INCOMPLETE 标记行前内容丢弃** — 强化提示 + 加 update 确认的 few-shot 示例
16. **add daily/weekly 去掉校验** — 后面跟什么都直接加进去
17. **用户文案友好化** — 去掉"三层覆盖"等技术术语，修复错别字
18. **思考折叠框** — NLP 翻译保留 thinking，与回复合并为一条卡片消息
19. **工具调用记录** — thinking 中包含调用了哪些函数
20. **hfai 任务名防并发** — task_name 加时间戳后缀
21. **show 输出** — "默认集群"改为"支持的集群"
22. **build_force 写入 prompt 翻译要点**
23. **log 开头重复信息去除**

### TODO（待处理）：

- [ ] SSH 连接异常时未关闭（build_wheel/test_repo 最外层 except 没有 ssh.close）
- [ ] task_status.json 跨进程并发不安全（threading.Lock 对 multiprocessing 无效）
- [ ] msg_cache（SQLite）无过期清理
- [ ] reply_cache（文件系统）无过期清理
- [ ] time_task.py 启动时 schedule.run_all() 立即执行所有任务
- [ ] update daily 的 parse_command 校验没传 dry_run
- [ ] _reply_with_thinking 没有 reply_message_markdown 的换行处理逻辑（目前无明显问题，观察中）
- [ ] config.py 硬编码敏感信息（代理密码、API key）
- [ ] build_wheel/test_repo 大量重复代码可提取公共函数
