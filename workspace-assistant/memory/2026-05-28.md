# 2026-05-28 工作日志

## 营养追踪与食谱系统设计

- 管理员提出新需求：每周食谱图片 → 每日午餐推荐 → 早晚餐拍照记录 → Dashboard 卡片
- 创建完整设计文档 `projects/ac/ac-nutrition-system-design-V1.0-DRAFT.md`
- 拆分为 4 个 Phase，20 个 TODO
- Phase 17-1：数据层 + 卡片骨架（9 TODO，N1-N9）
- Phase 17-2：Cron + 智能推荐（4 TODO，N10-N13）
- Phase 17-3：图片分析 + 健康档案（7 TODO，N14-N20）
- Phase 17-4：高级特性（可选，N21-N25）
- 更新 pipeline-state.json（todos + designDoc + phase + stage:idle）
- 更新 registry + changelog + MEMORY.md
- 待管理员审阅确认后转 ACTIVE，Build喵 可 pick up

## 18:40 — 清 nutrition 旧 TODO + 设计铁律

### 问题发现
营养系统设计文档（ac-nutrition-system-design-V1.0-DRAFT.md）和卡片 5 层架构是不同时期产物：
- 营养设计在前（独立 `user-data/nutrition/` + 自定义 API + OpenClaw cron）
- 卡片架构在后（`daily-data/` + apply_rules + heartbeat.json）
- 两个设计互不知道对方存在

### 修复
1. **pipeline-state.json**：移除 20 个旧 N1-N20 TODO，stage → idle，designDoc → null
2. **WORKFLOW.md**：新增「新功能设计铁律」章节
   - 强制设计前检查现架构（daily-data 结构、apply_rules、heartbeat.json）
   - 卡片统一 5 层：data.json / rules.json / display.json / prompt.json / heartbeat.json
   - 禁止三件事：自建目录、自定义 API、OpenClaw cron
   - 设计文档模板（兼容性 + 数据模型 + 规则 + cron + prompt + 前端映射）

### 待办
- 管理员稍后通过网页重新描述营养需求，届时按新架构重新设计

## 18:42 — 清理 Build喵 所有引用 + 澄清目录规则

### 清理范围
- **WORKFLOW.md**：移除「Build喵 流水线」整节（42-80行），启动清单去掉 pipeline-state
- **AGENTS.md**：移除 Build喵 流水线、pipeline-state 读取、Build喵 心跳流程
- **SOUL.md**：移除「Build喵 联动铁律」整节
- **HEARTBEAT.md**：整文件重写为最小化（仅保留 HEARTBEAT_OK 规则）
- **MEMORY.md**：Build喵 系统设计整节 → 一行「已暂停」说明

### 目录规则澄清
- ❌ 禁止：`user-data/some-standalone-system/` 这种自建独立目录
- ✅ 允许：`daily-data/nutrition/images/`、`daily-data/recipe/data/weekly/` 等卡片内子目录
- 规则含义：必须挂到卡片架构下，但卡片内部可以自由组织

## 18:46 — 设计规则场景隔离

### 问题
WORKFLOW.md 里的「新功能设计铁律」每次 session 都加载，会污染普通对话行为。
管理员指出：这个 agent 同时服务普通对话和 Daily Dashboard，规则不应全局生效。

### 修复
- 设计规则从 WORKFLOW.md 移到独立文件 `DESIGN.md`
- WORKFLOW.md 改为「场景路由」：普通聊天不加载 DESIGN.md，只有设计场景才读
- 新增文件列表加 DESIGN.md（项目文件，librarian 命名规范）

## 19:08-19:39 — 卡片喵 agent + Web Chat 模型拆分

### 创建 card-assistant agent
- 新建 `workspace-card-assistant/`，SOUL 定位于「数据操作员」而非「设计师」
- 铁律：只回应实际说的、先读后写、改了必刷 display、简洁确认
- 配置到 openclaw.json，model 使用 deepseek/deepseek-v4-pro
- `fileserver.py`：删 `_build_card_prompt()`，Daily 会话路由到 CARD_AGENT_MODEL
- 前端：选 Daily 会话自动切 agentId → card-assistant

### Web Chat 模型拆分
- 创建 `webchat-v4` (DeepSeek V4 Pro) 和 `webchat-opus` (Claude Opus 4) 两个 agent
- 共用 `workspace-webchat/`，SOUL 写入通用经验（身份、能力、铁律、用户信息、文件路径）
- 前端加一键切换按钮 `⚡V4` `🧠Opus`，同一会话切换保留上下文
- 删除设置面板入口按钮（⚙️），JWT 自动登录替代手动输 token

### 修复 doc-reader 权限
- assistant agent 缺少 `subagents.allowAgents`，补上 `["doc-reader"]`
