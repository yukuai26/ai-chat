
## 2026-06-03 修复 Web Chat 登录页"⏳ JS加载中"卡死 + 代码仓库对齐

### 问题
线上 `/var/www/chat/index.html` 登录页卡在"⏳ JS加载中...",JS未执行到"✅ JS已加载"。

### 根因
主script块(3943-9604)有 **2处多余的 `}`**(doLogin 4079行 / showToast 7129行附近),
导致整段5660行JS语法崩溃(`Unexpected token '}'`),jsDebug永远停在加载中。
用 `node --check` 抽块语法检查精确定位。

### 修复
sudo sed 删除2处多余 `}` → 部署文件 9686→9684行 → node --check 通过。

### 代码仓库对齐(以线上为准)
- 发现三版本分叉: 代码仓库 repo(10916行) / 线上(9684行) / 数据备份backup-repo
- 管理员定: **以线上为准**, issue-439营养功能是错误的要扔
- 操作: repo切main → 丢弃营养未提交改动 → 线上覆盖 index.html+fileserver.py → commit 15691ec
- 远程main被Build喵的营养commit(#458/#459)领先 → 管理员确认 force push 覆盖
- ✅ force push成功, 远程main = 线上干净版。营养代码保留在 feature/issue-438/439 本地分支

### 我犯的错(教训)
1. 一开始没查全就断言"workspace-assistant没有git仓库,从来没commit过" → 错误。
   实际代码在 workspace-build-cat/repo, 数据在 backup-repo。
   违反核心教训#2/#3(没查全就断言)。管理员两次纠正(backup vs 代码)才找对。
2. 拿 cloudflared 日志里的历史域名(courage-)当当前域名 → 应先 curl 实测HTTP状态码。
   实际真身一直是 yoga- 那个。
